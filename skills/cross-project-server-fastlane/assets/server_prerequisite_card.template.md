# Server Prerequisite Card

task:
owner/project/job tags:
local project root:
server project root:
transport mechanism:
fixed input/evidence object:

required files:

large artifacts:

server-visible proof:

runtime proof:

output root:
output root absent proof:

expected artifacts:

status source:

fallback condition:

finalizer command:

claim boundary:

BLOCK/HOLD/ALLOW:
